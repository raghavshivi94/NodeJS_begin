const num1 =5
const num2=10

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

addValues()

//if we have function that we invoked it doest need to be called from app.js and only need to require the module